apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotstar.fullname" . }}
  labels:
    {{- include "hotstar.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hotstar.selectorLabels" . | nindent 4 }}
